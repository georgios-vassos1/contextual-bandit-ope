Run a fast smoke-test experiment on task 11 (625 samples, 4 features, 3 arms) with minimal settings.

```bash
uv run pcmabinf run --task-id 11 --simulations 4 --batch-count 10 --batch-size 50 --n-jobs 2
```
